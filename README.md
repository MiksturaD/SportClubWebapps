# 🏋️ Спортивный клуб - Telegram Web App

Веб-приложение для управления спортивным клубом через Telegram Web App с системой ролей для администраторов и родителей.

## 📋 Функциональность

### Для всех пользователей:
- Просмотр информации о спортивных группах
- Просмотр расписания тренировок
- Просмотр цен на абонементы

### Для администраторов:
- 📝 Регистрация участников (ФИО, телефон родителя, дата рождения, мед справка, скидки)
- 📅 Управление расписанием тренировок
- 💰 Система учета оплаты
- 🎫 Управление скидками и акциями

### Для родителей/участников:
- 📞 Связь с администрацией
- 💳 Оплата занятий
- 🔄 Запрос на перенос занятия

## 🏗️ Структура проекта

```
SportClubWebapps/
├── app.py                 # Основной файл Flask приложения
├── config.py             # Конфигурация приложения
├── models.py             # Модели базы данных
├── requirements.txt      # Зависимости Python
├── templates/
│   └── index.html       # Главная страница
├── static/
│   └── js/
│       └── app.js       # JavaScript логика
├── env_example.txt      # Пример переменных окружения
└── README.md           # Документация
```

## 🚀 Установка и запуск

### 1. Клонирование и настройка окружения

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd SportClubWebapps

# Создайте виртуальное окружение
python -m venv venv

# Активируйте виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` на основе `env_example.txt`:

```bash
# Скопируйте пример
cp env_example.txt .env

# Отредактируйте .env файл
```

Обязательные настройки в `.env`:
- `ADMIN_TELEGRAM_ID` - ID администратора в Telegram
- `TELEGRAM_BOT_TOKEN` - токен вашего Telegram бота
- `TELEGRAM_WEBAPP_URL` - URL вашего приложения (для ngrok)

### 3. Создание Telegram бота

1. Найдите @BotFather в Telegram
2. Создайте нового бота командой `/newbot`
3. Получите токен бота
4. Добавьте Web App командой `/setmenubutton`:
   ```
   /setmenubutton
   @your_bot_username
   Спортивный клуб
   https://your-domain.ngrok.io
   ```

### 4. Запуск с ngrok

```bash
# Запустите Flask приложение
python app.py

# В другом терминале запустите ngrok
ngrok http 5000

# Скопируйте HTTPS URL из ngrok и обновите TELEGRAM_WEBAPP_URL в .env
```

### 5. Тестирование

1. Откройте вашего бота в Telegram
2. Нажмите на кнопку "Спортивный клуб"
3. Приложение откроется в Telegram Web App

## 📊 База данных

Приложение использует SQLite базу данных со следующими таблицами:

- **User** - пользователи системы
- **Participant** - участники спортивных групп
- **SportGroup** - спортивные группы
- **Schedule** - расписание тренировок
- **Subscription** - абонементы участников
- **Payment** - платежи
- **Discount** - скидки и акции
- **LessonTransfer** - переносы занятий

База данных создается автоматически при первом запуске приложения.

## 🎯 Спортивные группы

По умолчанию создаются следующие группы:
- Дзюдо младшая группа (6-10 лет)
- Дзюдо старшая группа (11-16 лет)
- Гимнастика
- ММА
- Женский фитнес

## 🔐 Система ролей

### Администратор
- Telegram ID указан в `ADMIN_TELEGRAM_ID`
- Полный доступ ко всем функциям управления
- Может регистрировать участников, управлять расписанием, отслеживать платежи

### Родитель/Участник
- Все остальные пользователи
- Доступ к информации о группах
- Возможность оплаты и запроса переноса занятий

## 🛠️ API Endpoints

### Основные:
- `GET /` - главная страница
- `POST /api/init` - инициализация пользователя
- `GET /api/sport-groups` - список спортивных групп
- `GET /api/schedule/<group_id>` - расписание группы

### Административные:
- `GET/POST /api/admin/participants` - управление участниками
- `GET/POST /api/admin/schedule` - управление расписанием
- `GET /api/admin/payments` - просмотр платежей

### Для родителей:
- `GET /api/parent/contact` - контактная информация
- `POST /api/parent/payment` - создание платежа
- `POST /api/parent/transfer` - запрос переноса занятия

## 🎨 Интерфейс

Приложение использует Telegram Web App API для интеграции с Telegram:
- Адаптивный дизайн
- Поддержка тем Telegram
- Модальные окна для форм
- Уведомления через Telegram API

## 🔧 Разработка

### Добавление новых функций:

1. **Модель данных** - добавьте в `models.py`
2. **API endpoint** - добавьте в `app.py`
3. **Frontend** - обновите `templates/index.html` и `static/js/app.js`

### Тестирование:

```bash
# Запуск в режиме разработки
python app.py

# Приложение будет доступно на http://localhost:5000
```

## 📱 Использование

### Для администратора:
1. Войдите в приложение с Telegram ID администратора
2. Используйте административную панель для управления
3. Регистрируйте участников, настраивайте расписание
4. Отслеживайте платежи и статистику

### Для родителей:
1. Откройте приложение через бота
2. Просматривайте информацию о группах
3. Оплачивайте занятия
4. Запрашивайте перенос занятий при необходимости

## 🚨 Безопасность

- Все административные функции защищены проверкой роли
- Валидация данных на сервере
- Безопасное хранение сессий
- CORS настройки для Web App

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи Flask приложения
2. Убедитесь в правильности настроек в `.env`
3. Проверьте доступность ngrok туннеля
4. Убедитесь в корректности Telegram Bot Token

## 📄 Лицензия

Этот проект создан для демонстрации возможностей Telegram Web App API.
